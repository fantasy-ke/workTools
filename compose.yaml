services:
  worktools-web:
    build:
      context: .
      dockerfile: Dockerfile
    image: worktools-web:local
    ports:
      - "${WORKTOOLS_PORT:-8080}:80"
    volumes:
      - ./data/docker/nginx-logs:/var/log/nginx
    restart: unless-stopped
